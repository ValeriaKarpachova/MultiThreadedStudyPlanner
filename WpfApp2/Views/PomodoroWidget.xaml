<UserControl x:Class="WpfApp2.Views.PomodoroWidget"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <!-- Стиль кнопки що повністю ігнорує MaterialDesign -->
        <Style x:Key="PomBtn" TargetType="Button">
            <Setter Property="Width"           Value="32"/>
            <Setter Property="Height"          Value="28"/>
            <Setter Property="Margin"          Value="2"/>
            <Setter Property="FontSize"        Value="14"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <!-- TextBlock замість ContentPresenter — гарантує білий колір -->
                            <TextBlock Text="{TemplateBinding Content}"
                                       Foreground="White"
                                       FontSize="{TemplateBinding FontSize}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd"
                                        Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd"
                                        Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border CornerRadius="8" Background="#F5F4FF" Padding="8,6">
        <StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="🍅 " FontSize="12"/>
                <TextBlock x:Name="PhaseLabel" FontSize="12"
                           FontWeight="Bold" Foreground="#534AB7"/>
            </StackPanel>

            <TextBlock x:Name="TimerLabel" FontSize="22" FontWeight="Bold"
                       HorizontalAlignment="Center" Margin="0,2"/>

            <ProgressBar x:Name="TimerBar" Height="4"
                         Minimum="0" Margin="0,2,0,6"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button x:Name="StartBtn"
                        Style="{StaticResource PomBtn}"
                        Background="#534AB7"
                        Content="▶" ToolTip="Старт"
                        Click="Start_Click"/>
                <Button Style="{StaticResource PomBtn}"
                        Background="#534AB7"
                        Content="⏸" ToolTip="Пауза"
                        Click="Pause_Click"/>
                <Button Style="{StaticResource PomBtn}"
                        Background="#534AB7"
                        Content="↺" ToolTip="Скинути"
                        Click="Reset_Click"/>
                <Button Style="{StaticResource PomBtn}"
                        Background="#534AB7"
                        Content="⏭" ToolTip="Пропустити фазу"
                        Click="Skip_Click"/>
            </StackPanel>

            <TextBlock x:Name="CountLabel" FontSize="10" Foreground="Gray"
                       HorizontalAlignment="Center"/>
        </StackPanel>
    </Border>
</UserControl>